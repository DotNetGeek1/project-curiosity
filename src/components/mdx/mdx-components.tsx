import { Callout } from '@/components/ui/callout';
import { Quote } from '@/components/ui/quote';

/**
 * Components made available to every MDX document. Keep this list small and
 * narrative-focused: anything added here becomes part of the authoring API.
 */
export const mdxComponents = {
  Callout,
  Quote,
};
